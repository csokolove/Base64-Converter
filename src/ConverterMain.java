public class ConverterMain {
    public static void main(String[] args) {
        Parser parser = new Parser();
        // TODO Validate that there are arguments
        parser.parseArguments(args);
    }   
}
